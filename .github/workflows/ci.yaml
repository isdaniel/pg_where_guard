name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        pg-version: [14, 15, 16, 17, 18]
      fail-fast: false

    env:
      PG_VER: ${{ matrix.pg-version }}
      PG_BIN: /usr/lib/postgresql/${{ matrix.pg-version }}/bin

    steps:
      - uses: actions/checkout@v4

      - name: Cache cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-pg${{ matrix.pg-version }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-pg${{ matrix.pg-version }}-

      - name: Install PostgreSQL ${{ env.PG_VER }}
        run: |
          sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            clang \
            llvm \
            libreadline-dev \
            zlib1g-dev \
            postgresql-${{ env.PG_VER }} \
            postgresql-server-dev-${{ env.PG_VER }}

      - name: Install cargo-pgrx
        run: cargo install cargo-pgrx --version 0.18.0

      - name: Initialize pgrx
        run: cargo pgrx init --pg${{ env.PG_VER }} ${{ env.PG_BIN }}/pg_config

      - name: Set permissions
        run: |
          sudo chmod 777 /usr/share/postgresql/${{ env.PG_VER }} -R
          sudo chmod 777 /usr/lib/postgresql/${{ env.PG_VER }} -R

      - name: Run clippy
        run: cargo clippy --no-default-features --features pg${{ env.PG_VER }} -- -D warnings

      - name: Run tests
        run: cargo pgrx test --release pg${{ env.PG_VER }}

      - name: Package extension
        run: cargo pgrx package --pg-config=${{ env.PG_BIN }}/pg_config
